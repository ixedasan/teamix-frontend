query FindProjectById {
	findProjectById {
		id
		name
		icon
		description
		cover
		members {
			id
			userId
			projectId
			user {
				username
				displayName
				avatar
				email
			}
			role
			createdAt
		}
	}
}
